# Redirect — canonical name is `montfortre`

This folder (`ghl-react`) is the **physical git repo**. Use either path:

- `d:\Montfortr\montfortre\` — canonical (junction when created)
- `d:\Montfortr\ghl-react\` — same files

See `PROJECT-CONTEXT.md` and `RENAME-PENDING.md`.
